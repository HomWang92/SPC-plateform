server:
  port: 10000
spring:
  application:
    name: SPC-demo
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8001
#        metadata:
#          user:
#            name: name
#            password: password
#            management:
#              context-path: /admin/actuator
#      config:
#        server-addr: localhost:8001
#        group: DEFAULT_GROUP
#        prefix: SPC-demo
#        file-extension: yaml

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS